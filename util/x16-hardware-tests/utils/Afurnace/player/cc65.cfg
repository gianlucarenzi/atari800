MEMORY {
    ZP:   start = $0000, size = $0100, type = rw;
    CODE: start = $3000, size = $0800, type = rw;
}

SEGMENTS {
    CODE:   load = CODE, type = ro;
    RODATA: load = CODE, type = ro;
    DATA:   load = CODE, type = rw;
    BSS:    load = CODE, type = bss, optional = yes;
}
