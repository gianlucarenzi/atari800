MEMORY {
    ZP:  start = $80, size = $70, type = rw;
    RAM: start = $2000, size = $8000, type = rw;
}
SEGMENTS {
    EXEHDR: load = RAM, type = ro;
    STARTUP: load = RAM, type = ro;
    VECTORS: load = RAM, type = ro;
}
